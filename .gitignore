# ---------------------------------------------------------------------------
# Patient data is governed by the PhysioNet Credentialed Health Data Use
# Agreement and MUST NOT be committed. Everything under data_private/ is
# derived from eICU-CRD and stays local.
# ---------------------------------------------------------------------------
data_private/
*.csv

# Analysis objects (regenerate with run_all.R)
results/*.rds
results/*.log
*.rds

# Rendered outputs that are regenerated from source
submission/figures_png/

# R
.Rhistory
.RData
.Rproj.user/
*.Rproj

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# LaTeX / Office scratch
*.aux
*.log
*.out
~$*

# Internal working notes and scratch literature exports, not part of the analysis
CLAUDE.md
*-set.txt

# ---------------------------------------------------------------------------
# Generated outputs are NOT committed. They are products of the pipeline, not
# source, and every one is regenerated by `Rscript run_all.R`. Committing them
# invites the repository and the manuscript to drift apart silently.
# ---------------------------------------------------------------------------
# all TIFFs, regenerated by R/16_figures.R
figures/
# regenerated by R/12_tables.R
docs/TABLES.md
# analysis objects and the run log
results/
# built from docs/TABLES.md and figures/
submission/Tables.docx
submission/Figures.docx

# The cover letter is written for the editors of one journal, not for the field.
# It stays local and out of the public repository.
submission/Cover_Letter.md
submission/Cover_Letter.docx
